@model IEnumerable<JassWeather.Models.JassVariableGroupRel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JassVariableGroup.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JassVariable.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JassVariableGroup.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JassVariable.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JassVariableGroupRelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.JassVariableGroupRelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JassVariableGroupRelID })
        </td>
    </tr>
}

</table>
